# Mark binary files so git doesn't try to diff them as text.
# GGUF model weights are excluded via .gitignore.
*.sf2   -diff -text
*.pdf   -diff -text
*.png   -diff -text
*.ico   -diff -text
*.woff  -diff -text
*.woff2 -diff -text
*.gguf filter=lfs diff=lfs merge=lfs -text
